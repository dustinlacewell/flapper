# SetFields

<Doc />
<Proc />

## Parameters

<Params />

## Example

<Code language="javascript">{`SetFields({
    'title': (context, asset) => formatAsTitle(asset.slug),
    'address': (context, asset) => normalizeAddress(asset.address),
})`}</Code>
