# MakeMenu

<Doc />
<Proc />

## Parameters

<Params />

## Example

Create menu nodes for assets, with their name based on each asset's slug, each node attached to the `docs` node as children.

```javascript
MakeMenu("{{slug}}", "docs")
```
