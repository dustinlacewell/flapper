# SourceMatchedFiles

<Doc />
<Proc />

## Parameters

<Params />

## Example

Match all files, capturing `category`, `slug` and `ext` properties:

<Code language="javascript">{`SourceMatchedFiles('posts/{{category}}/{{slug}}.{{ext}}')`}</Code>
